# 管理员登录修复

> 日期：2026-03-15
> 范围：管理员密码哈希修复

## 问题原因
- 之前在 PowerShell 执行更新时 `$` 被变量展开，导致写入的密码哈希缺少前缀（非合法 BCrypt）。

## 处理
- 使用 Here-String 重新写入完整 BCrypt 哈希。
- 数据库当前 admin 密码为 123456。

## 影响
- 登录账号：admin / 123456 可正常使用。
