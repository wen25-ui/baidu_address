# 静态图标路径修复与旧构建清理

> 日期：2026-03-15
> 范围：tabBar 图标路径缺失修复，清理旧构建产物

## 变更概述
1. 新增 `src/static/icons/`，补齐 `home/order/user` 的 tabBar 图标文件。
2. 同步补齐 `dist/dev/mp-weixin/static/icons/`，确保开发工具立即可用。
3. 删除旧构建目录 `frontend/dist/build`，避免误用旧版本。

## 影响
- 解决 `app.json` 中 tabBar 图标缺失报错。
- 避免微信开发者工具打开旧构建导致持续报错。
