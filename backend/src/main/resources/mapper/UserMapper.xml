<mapper namespace="com.graduation.platform.repository.UserRepository">
    <resultMap id="UserResultMap" type="com.graduation.platform.model.entity.User">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="email" property="email" />
        <result column="phone" property="phone" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <select id="findById" resultMap="UserResultMap">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="UserResultMap">
        SELECT * FROM users
    </select>

    <insert id="insert" parameterType="com.graduation.platform.model.entity.User">
        INSERT INTO users (username, password, email, phone, created_at, updated_at)
        VALUES (#{username}, #{password}, #{email}, #{phone}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update" parameterType="com.graduation.platform.model.entity.User">
        UPDATE users
        SET username = #{username}, password = #{password}, email = #{email}, phone = #{phone}, updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="int">
        DELETE FROM users WHERE id = #{id}
    </delete>
</mapper>